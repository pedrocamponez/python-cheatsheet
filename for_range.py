# range(start, stop, step)
# start = where the iteration will start
# stop = where the iteration will stop
# step = the "i += 1", steps to go through the range

numbers = range(1, 10, 2)

for number in numbers:
    print(number)